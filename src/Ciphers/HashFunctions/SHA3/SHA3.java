package Ciphers.HashFunctions.SHA3;

import Ciphers.Basic.HashFunction;

public class SHA3 implements HashFunction {
    @Override
    public byte[] process(byte[] input) {
        return new byte[0];
    }

    @Override
    public void reset() {

    }

    @Override
    public int getLength() {
        return 0;
    }
}
